<UserControl x:Class="OrdersAndisheh.View.ImportColumnMatcher"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="110" MinWidth="100">
    <Grid>
        <StackPanel Orientation="Vertical"  FlowDirection="RightToLeft">
            <Label Content="{Binding Header}" Height="26" FlowDirection="RightToLeft"  Margin="10" />
            <ComboBox ItemsSource="{Binding ImportingData}" SelectedValue="{Binding MatchName}" Margin="10" />
        </StackPanel>
    </Grid>
</UserControl>
